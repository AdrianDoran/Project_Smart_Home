export class Device{
    email: String;
    name: String;
    data: Array<Int16Array>; // May need changing according to the data. This should not be the main portion of the data -> just for local storage.
    

}