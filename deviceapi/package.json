{
  "name": "deviceapi",
  "version": "1.0.0",
  "description": "API for handling devicedata.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -r dotenv/config api.js",
    "doc": "apidoc -o public/generated-docs/ -e node_modules/",
    "now-init": "now -p && now alias",
    "deploy": "now remove affordable-smarthome -y && now -p && now alias"
  },
  "author": "Adrian Doran and Ben Hodgson",
  "license": "ISC",
  "dependencies": {
    "apidoc": "^0.17.7",
    "cors": "^2.8.5",
    "now": "^16.2.0",
    "url-search-params-polyfill": "^7.0.0"
  },
  "devDependencies": {
    "body-parser": "^1.20.1",
    "dotenv": "^8.1.0",
    "express": "^4.18.2",
    "mongoose": "^5.7.1",
    "nodemon": "^1.19.2"
  }
}
